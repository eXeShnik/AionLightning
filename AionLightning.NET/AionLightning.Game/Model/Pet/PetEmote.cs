namespace AionLightning.Game.Model.Pet;

/// <summary>Toy-pet emote ids used by SM_PET_EMOTE (Java model.gameobjects.PetEmote).</summary>
public enum PetEmote
{
    MoveStop = 0,
    MoveTo = 12,
    Alarm = -114,
    Fly = -125,
    Unknown = 255,
}
