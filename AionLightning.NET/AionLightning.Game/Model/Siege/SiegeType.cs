namespace AionLightning.Game.Model.Siege;

/// <summary>Java model.siege.SiegeType. Values match the Java typeId ordinals exactly.</summary>
public enum SiegeType
{
    FORTRESS = 0,
    ARTIFACT = 1,
    BOSSRAID_LIGHT = 2,
    BOSSRAID_DARK = 3,
    INDUN = 4,
    UNDERPASS = 5,
    SOURCE = 6,
}
