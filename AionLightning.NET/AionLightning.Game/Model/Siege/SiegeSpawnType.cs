namespace AionLightning.Game.Model.Siege;

/// <summary>Java model.siege.SiegeSpawnType. Values match the Java id ordinals exactly.</summary>
public enum SiegeSpawnType
{
    PEACE = 0,
    GUARD = 1,
    ARTIFACT = 2,
    PROTECTOR = 3,
    MINE = 4,
    PORTAL = 5,
    GENERATOR = 6,
    SPRING = 7,
    RACEPROTECTOR = 8,
}
