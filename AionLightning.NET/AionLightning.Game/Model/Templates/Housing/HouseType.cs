namespace AionLightning.Game.Model.Templates.Housing;

/// <summary>Java model.templates.housing.HouseType — the "size" attribute of a &lt;building&gt;
/// element in house_buildings.xml. Names match the XML values verbatim.</summary>
public enum HouseType
{
    ESTATE,
    MANSION,
    HOUSE,
    STUDIO,
    PALACE,
}
