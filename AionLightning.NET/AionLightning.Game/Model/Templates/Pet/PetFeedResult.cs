namespace AionLightning.Game.Model.Templates.Pet;

/// <summary>One &lt;result&gt; reward-item candidate from pet_feed.xml (Java model.templates.pet.PetFeedResult).</summary>
public sealed record PetFeedResult(int Item, string? Name);
